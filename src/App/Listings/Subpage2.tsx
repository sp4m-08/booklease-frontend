import React from "react";

const Subpage2 = () => {
  return (
    <div className="flex justify-center items-center h-screen w-screen">
      add subpage here then import it to respective file on pages. will require
      this format later on for more pages. just my standard practice for project
      readability.
    </div>
  );
};

export default Subpage2;
